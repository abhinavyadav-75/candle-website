[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
dependsOn = ["setup"]
cmds = ["cd Asset-Manager && npm ci || npm install"]

[phases.build]
dependsOn = ["install"]
cmds = ["cd Asset-Manager && npm run build"]

[start]
cmd = "cd Asset-Manager && node dist/index.cjs"
