services:
  - type: web
    name: candle-website
    runtime: node
    rootDir: Asset-Manager
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
